rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Auth is now handled client-side via passcode.
    // All reads and writes are open — the app enforces access control in the browser.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
